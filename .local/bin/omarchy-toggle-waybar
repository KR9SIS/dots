#!/usr/bin/env bash

if systemctl is-active --user waybar.service -q; then
  echo "Sending signal"
  pkill -x .waybar-wrapped --signal SIGUSR1
else
  echo "Restarting waybar"
  systemctl restart --user waybar.service
fi
